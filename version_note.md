1. longest_gene.pl
方法：使用原始方法搜尋序列，並挑選最長的一段。
特色：程式簡單，能快速找到符合條件的片段。
限制：找到的編碼區偏短，很可能並非想要的結果。

2. longest_gene_regex.pl
方法：用 Regex 來比對，搭配 %3==0 篩選長度是否為 3 的倍數。
特色：展示正規表達式的應用，程式碼簡潔。
限制：同樣缺乏逐 codon 掃描，無法完整處理多個 ATG 起點。

3. exam1.pl
方法：為longest_gene改寫，把所有符合 ATG 開頭、TAA 結尾的片段全列出來。
特色：符合題目找最長的直觀要求。
限制：這段程式能列出一些符合 ATG→TAA 的序列，但不是所有可能的最長 ORF。

4. longest_gene_scan_each.pl
方法：逐 codon 掃描，每遇到 ATG 就往後以 3 個字母為單位尋找最近的終止子。
特色：
能正確處理巢狀與重疊 ORF。
結果更符合生物學定義。
限制：程式較複雜，執行時間可能比 Regex 慢。

5. exam9.pl
方法：一個if迴圈對應一個閱讀框架，共三個。	
特色：有發現偏移問題，其實Regex無法使用在本主題上，因為限制過多。
限制：輸出沒換行但影響不大。

6. exam9_2.pl
在前者加上每70換行輸出，還有拿掉區域變數改全域。

7. exam9_detial.pl.pl
~~媽的檔案能不能好好取個名字啊~~
為甚麼都變成組員的檔名呢，原因是我開擺，其實是我看到能跑(^上面那個)就懶得改了，沒他那麼勤奮。
每70一行改成60一行(根本沒差好嗎)。

8. exam12_annotated.pl
幹！我當初就給他for三個閱讀框的偽代碼！自己拆3個if的。請問現在是版本回溯嗎？為甚麼要繞一大圈。
對喇我的問題，因為我沒去吵3個if太慢對吧。